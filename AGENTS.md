# AGENTS.md

## Cursor Cloud specific instructions

### Repository state

This repository (`cli-creator-skills`) is a freshly initialized project with no source code, dependencies, or services yet. It contains only a `README.md`.

### Development environment

- No package manager, language runtime, or framework has been chosen yet.
- No build, lint, or test commands exist.
- No services need to be started.

### For future agents

- Once the tech stack is established, update this section with:
  - How to install dependencies (update script)
  - How to run lint, tests, and the application
  - Any non-obvious setup caveats
- The update script (`SetupVmEnvironment`) is currently a no-op (`echo "No dependencies to install"`). Replace it once a `package.json`, `requirements.txt`, or similar is added.
